<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.asset.tags.service.persistence.AssetTagFinder.countByG_N">
		<![CDATA[
			SELECT
				COUNT(DISTINCT AssetEntries_AssetTags.entryId) AS COUNT_VALUE
			FROM
				AssetTag
			INNER JOIN
				AssetEntries_AssetTags ON
					(AssetEntries_AssetTags.tagId = AssetTag.tagId)
			WHERE
				(AssetEntries_AssetTags.entryId IN (
					SELECT
						entryId
					FROM
						AssetEntry
					WHERE
						(AssetEntry.groupId = ?) AND
						(AssetEntry.visible = [$TRUE$])
				)) AND
				(AssetTag.name LIKE ?)
		]]>
	</sql>
	<sql id="com.liferay.asset.tags.service.persistence.AssetTagFinder.countByG_C_N">
		<![CDATA[
			SELECT
				COUNT(DISTINCT AssetEntries_AssetTags.entryId) AS COUNT_VALUE
			FROM
				AssetTag
			INNER JOIN
				AssetEntries_AssetTags ON
					(AssetEntries_AssetTags.tagId = AssetTag.tagId)
			WHERE
				(AssetEntries_AssetTags.entryId IN (
					SELECT
						entryId
					FROM
						AssetEntry
					WHERE
						(AssetEntry.groupId = ?) AND
						(AssetEntry.classNameId = ?) AND
						(AssetEntry.visible = [$TRUE$])
				)) AND
				(AssetTag.name LIKE ? OR ? IS NULL)
		]]>
	</sql>
	<sql id="com.liferay.asset.tags.service.persistence.AssetTagFinder.findByG_C_N">
		<![CDATA[
			SELECT
				DISTINCT {AssetTag.*}
			FROM
				AssetTag
			INNER JOIN
				AssetEntries_AssetTags ON
					(AssetEntries_AssetTags.tagId = AssetTag.tagId)
			WHERE
				(AssetEntries_AssetTags.entryId IN (
					SELECT
						entryId
					FROM
						AssetEntry
					WHERE
						(AssetEntry.groupId = ?) AND
						(AssetEntry.classNameId = ?) AND
						(AssetEntry.visible = [$TRUE$])
				)) AND
				(AssetTag.name LIKE ? OR ? IS NULL)
			ORDER BY
				AssetTag.name ASC
		]]>
	</sql>
	<sql id="com.liferay.asset.tags.service.persistence.AssetTagFinder.findByG_N_S_E">
		<![CDATA[
			SELECT
				AssetTag.tagId, AssetTag.name, SUM(SocialActivityCounter.currentValue)
			FROM
				AssetTag
			INNER JOIN
				AssetEntries_AssetTags ON
					(AssetEntries_AssetTags.tagId = AssetTag.tagId)
			INNER JOIN
				AssetEntry AssetEntry ON
					(AssetEntry.entryId = AssetEntries_AssetTags.entryId)
			INNER JOIN
				SocialActivityCounter ON
					(SocialActivityCounter.classNameId = AssetEntry.classNameId) AND
					(SocialActivityCounter.classPK = AssetEntry.classPK)
			WHERE
				(SocialActivityCounter.groupId = ?) AND
				(SocialActivityCounter.name = ?) AND
				(SocialActivityCounter.startPeriod >= ?) AND
				(SocialActivityCounter.startPeriod <= ?) AND
				(SocialActivityCounter.startPeriod + ? <= ?)
			GROUP BY
				AssetTag.tagId, AssetTag.name
		]]>
	</sql>
</custom-sql>